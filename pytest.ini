[pytest]
# `tests` is on the path so shared scaffolding (lock_helpers) is imported by
# configuration rather than by pytest's prepend-mode side effect, which would
# break under --import-mode=importlib or if tests/ ever gained an __init__.py.
pythonpath = . tests
testpaths = tests
python_files = test_*.py
