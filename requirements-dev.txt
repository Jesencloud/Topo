# The lint and test tooling, pinned so CI and a developer's machine agree.
#
# Unpinned, a ruff release turns an unrelated PR red (new rules, changed
# formatting) and `./check.sh` passes locally while CI fails, or the reverse.
# Both CI jobs install this file, so bumping a version here is a deliberate,
# reviewable commit that lands with whatever fixes the new version demands.
#
# Runtime dependency, not a dev tool: `packaging` is imported by src/, and CI
# installs it for the test job. It is pinned here for the same reason.
mypy==1.18.2
packaging==25.0
pytest==9.0.3
ruff==0.16.4
tach==0.35.0
vulture==2.16
